
            // an array is a collection of values into one
            int[] luckyNumbers = { 4, 8, 15, 16, 23, 42 };


            //below 5 mean the string can hold 5 values
            string[] friends = new string[5];
            friends[0] = "Jim"
            friends[1] = "tim"

                //below will print out 4 this is acessing the variable
            Console.WriteLine(luckyNumbers [0]);

            //  all values must be of the same type
